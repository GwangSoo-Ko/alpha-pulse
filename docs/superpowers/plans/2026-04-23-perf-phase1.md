# 성능 개선 Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Feedback/Content/Briefings 페이지의 로딩 속도를 구체적이고 측정 가능하게 개선. 감사 결과 기반 5 Major + 2 Quick Wins (QW-2 는 이미 구현됨 확인 → 스킵).

**Architecture:** (1) FeedbackEvaluator 를 request-scoped 로 전환해 안전한 record-level 캐싱 추가. (2) `get_all_analytics` one-pass 메서드로 4 스캔 → 1 스캔 축소. (3) BriefingStore 에 `json_extract` 기반 경량 `list_summaries` 추가. (4) ContentReader 에 mtime-TTL 캐시. (5) DB 인덱스 + offset 페이지네이션.

**Tech Stack:** Python 3.12 + FastAPI + Pydantic + SQLite (json_extract). FE 변경 최소 (revalidate 시도는 선택).

**Branch:** `perf/phase1-optimizations` (spec 커밋 `9a85758` 완료)

**Spec:** `docs/superpowers/specs/2026-04-23-perf-phase1-design.md`

**이미 구현됨**: Spec 의 QW-2 (pulse history 서버 ASC 정렬) 는 `alphapulse/webapp/api/market.py:113` 에 이미 `rows_asc = sorted(...)` 로 구현되어 있고 FE 에 재정렬 없음. 확인 후 스킵.

---

## File Structure

### Backend 변경
- **Modify:** `alphapulse/core/storage/feedback.py` — `_create_table` 에 인덱스 3개 추가, `get_recent(limit, offset=0)` 시그니처 확장
- **Modify:** `alphapulse/core/storage/briefings.py` — `get_recent(days, offset=0)` 시그니처 확장, 신규 `list_summaries(limit, offset)` 메서드
- **Modify:** `alphapulse/feedback/evaluator.py` — `_get_cached_records` 추가, 기존 메서드 4개 내부 사용 교체, 신규 `get_all_analytics(days)`
- **Modify:** `alphapulse/webapp/api/feedback.py` — `get_feedback_evaluator` dep 가 매 요청 새 인스턴스 생성, `/analytics` 가 `get_all_analytics` 호출
- **Modify:** `alphapulse/webapp/api/briefing.py` — 목록 엔드포인트가 `list_summaries` + offset 사용
- **Modify:** `alphapulse/webapp/store/readers/content.py` — `_scan` mtime-TTL 캐시
- **Modify:** `alphapulse/webapp/main.py` — `app.state.feedback_evaluator` 제거 (request-scoped 로 이동)

### 테스트
- **Modify:** `tests/feedback/test_evaluator.py` — 캐시 동작/get_all_analytics 골든 테스트
- **Modify:** `tests/core/storage/test_briefings.py` — offset + list_summaries 테스트
- **Modify:** `tests/feedback/test_store.py` — offset 테스트
- **Modify:** `tests/webapp/store/readers/` — ContentReader 캐시 테스트 (신규 파일 허용)
- **Modify:** `tests/webapp/api/test_feedback.py` — `/analytics` 회귀 유지

### Frontend (선택)
- **Modify:** `webapp-ui/app/(dashboard)/briefings/page.tsx`, `.../market/pulse/page.tsx` — revalidate 시도 (QW-3, 실패 시 롤백)

---

## Conventions

- 각 Task 완료 시 개별 커밋
- TDD: test first → red → implement → green → commit
- ruff clean 필수
- 변경 후 `pytest tests/ -q` 전체 회귀
- FE 변경은 `pnpm lint` + `pnpm build` 확인

---

## Task 1: DB 인덱스 추가 (QW-1)

**Files:**
- Modify: `alphapulse/core/storage/feedback.py`
- Modify: `tests/feedback/test_store.py`

- [ ] **Step 1.1: 테스트 추가**

`tests/feedback/test_store.py` 에 신규 테스트 추가:

```python
def test_indexes_exist(store):
    """hit_1d/3d/5d 에 partial 인덱스가 생성되어야 한다."""
    import sqlite3
    with sqlite3.connect(store.db_path) as conn:
        rows = conn.execute(
            "SELECT name FROM sqlite_master WHERE type='index' AND sql IS NOT NULL"
        ).fetchall()
    names = {r[0] for r in rows}
    assert "idx_feedback_hit_1d" in names
    assert "idx_feedback_hit_3d" in names
    assert "idx_feedback_hit_5d" in names
```

- [ ] **Step 1.2: Red**

Run: `pytest tests/feedback/test_store.py::test_indexes_exist -v`
Expected: FAIL — 인덱스 없음

- [ ] **Step 1.3: 구현**

`alphapulse/core/storage/feedback.py` 의 `_create_table` 메서드 끝부분 (마지막 `CREATE TABLE` 문 이후, `with ... conn:` 블록 안) 에 추가:

```python
            # Partial indexes on hit flags — skip NULL (unevaluated)
            conn.execute(
                "CREATE INDEX IF NOT EXISTS idx_feedback_hit_1d "
                "ON signal_feedback(hit_1d) WHERE hit_1d IS NOT NULL"
            )
            conn.execute(
                "CREATE INDEX IF NOT EXISTS idx_feedback_hit_3d "
                "ON signal_feedback(hit_3d) WHERE hit_3d IS NOT NULL"
            )
            conn.execute(
                "CREATE INDEX IF NOT EXISTS idx_feedback_hit_5d "
                "ON signal_feedback(hit_5d) WHERE hit_5d IS NOT NULL"
            )
```

현재 `_create_table` 의 위치 — 검색해서 `CREATE TABLE IF NOT EXISTS signal_feedback` 블록 바로 아래 동일 `with ... conn:` 안에 붙인다.

- [ ] **Step 1.4: Green**

Run: `pytest tests/feedback/test_store.py -v`
Expected: 전체 통과 (신규 포함).

- [ ] **Step 1.5: 린트 + 커밋**

```bash
ruff check alphapulse/core/storage/feedback.py tests/feedback/test_store.py
git add alphapulse/core/storage/feedback.py tests/feedback/test_store.py
git commit -m "perf: signal_feedback hit_1d/3d/5d partial 인덱스 추가"
```

---

## Task 2: ContentReader mtime-TTL 캐시

**Files:**
- Modify: `alphapulse/webapp/store/readers/content.py`
- Create: `tests/webapp/store/readers/test_content_cache.py` (기존 readers 디렉터리에 신규)

- [ ] **Step 2.1: 테스트 파일 생성**

```python
"""ContentReader mtime-based scan cache 테스트."""

from pathlib import Path

from alphapulse.webapp.store.readers.content import ContentReader


def _write_report(dir_path: Path, name: str, title: str = "T") -> None:
    (dir_path / name).write_text(
        f"---\ntitle: {title}\ncategory: 테스트\npublished: 2026-04-01\nanalyzed_at: 2026-04-02T10:00\n---\n\n본문\n",
        encoding="utf-8",
    )


def test_scan_cache_hit_uses_no_file_io(tmp_path, monkeypatch):
    _write_report(tmp_path, "a.md")
    reader = ContentReader(tmp_path)

    # 첫 호출: 실제 스캔
    first = reader._scan()
    assert len(first) == 1

    # iterdir 을 모킹해서 두 번째 호출이 디스크 접근 없어야 함
    call_count = {"n": 0}

    real_iterdir = Path.iterdir

    def counting_iterdir(self):
        call_count["n"] += 1
        return real_iterdir(self)

    monkeypatch.setattr(Path, "iterdir", counting_iterdir)

    second = reader._scan()
    assert second == first
    assert call_count["n"] == 0  # 캐시 히트: iterdir 호출 없음


def test_scan_cache_invalidates_on_new_file(tmp_path):
    _write_report(tmp_path, "a.md")
    reader = ContentReader(tmp_path)
    first = reader._scan()
    assert len(first) == 1

    # 새 파일 추가 → 디렉터리 mtime 변경
    import time
    time.sleep(0.01)  # fs mtime 해상도 우회
    _write_report(tmp_path, "b.md")

    second = reader._scan()
    assert len(second) == 2


def test_scan_returns_empty_for_missing_dir(tmp_path):
    missing = tmp_path / "nonexistent"
    reader = ContentReader(missing)
    assert reader._scan() == []
    # 두 번째 호출도 안전
    assert reader._scan() == []
```

- [ ] **Step 2.2: Red**

Run: `pytest tests/webapp/store/readers/test_content_cache.py -v`
Expected: `test_scan_cache_hit_uses_no_file_io` 만 FAIL (현재 구현은 매번 디스크 스캔). 다른 2개는 이미 통과할 수 있음.

- [ ] **Step 2.3: 구현**

`alphapulse/webapp/store/readers/content.py` 의 `ContentReader.__init__` 과 `_scan` 메서드를 다음과 같이 변경:

```python
class ContentReader:
    def __init__(self, reports_dir: Path | str) -> None:
        self.reports_dir = Path(reports_dir)
        self._scan_cache: list[ReportMeta] | None = None
        self._scan_cache_mtime: float | None = None

    def _scan(self) -> list[ReportMeta]:
        if not self.reports_dir.is_dir():
            # 디렉터리 없음 → 캐시 무효화 + 빈 반환
            self._scan_cache = None
            self._scan_cache_mtime = None
            return []

        try:
            dir_mtime = self.reports_dir.stat().st_mtime
        except OSError:
            dir_mtime = None

        if (
            self._scan_cache is not None
            and dir_mtime is not None
            and self._scan_cache_mtime == dir_mtime
        ):
            return self._scan_cache

        metas: list[ReportMeta] = []
        for entry in self.reports_dir.iterdir():
            if not entry.is_file():
                continue
            if entry.suffix != ".md":
                continue
            if entry.name.startswith("."):
                continue
            metas.append(_meta_from_file(entry))

        self._scan_cache = metas
        self._scan_cache_mtime = dir_mtime
        return metas
```

- [ ] **Step 2.4: Green**

Run: `pytest tests/webapp/store/readers/test_content_cache.py -v`
Expected: 3 passed.

- [ ] **Step 2.5: 회귀 확인**

Run: `pytest tests/webapp/store/readers/ tests/webapp/api/test_content.py -q`
Expected: 기존 테스트 모두 통과.

- [ ] **Step 2.6: 린트 + 커밋**

```bash
ruff check alphapulse/webapp/store/readers/content.py tests/webapp/store/readers/test_content_cache.py
git add alphapulse/webapp/store/readers/content.py tests/webapp/store/readers/test_content_cache.py
git commit -m "perf(content): ContentReader._scan mtime 기반 TTL 캐시"
```

---

## Task 3: FeedbackEvaluator request-scoped 전환

**Files:**
- Modify: `alphapulse/webapp/main.py`
- Modify: `alphapulse/webapp/api/feedback.py`
- Modify: `tests/webapp/api/test_feedback.py` (fixture 영향 확인)

- [ ] **Step 3.1: 기존 상태 확인**

Run:
```bash
grep -n "app.state.feedback_evaluator\|feedback_evaluator" alphapulse/webapp/main.py
grep -n "feedback_evaluator\|get_feedback_evaluator" alphapulse/webapp/api/
```

`main.py` 에서 싱글턴 생성/할당, `feedback.py` 의 `get_feedback_evaluator` 가 state 에서 참조 중. fixture 나 테스트가 `app.state.feedback_evaluator` 를 직접 setter 로 모킹하는지 확인:

```bash
grep -rn "app.state.feedback_evaluator" tests/
```

결과 파일 목록을 기억해둔다 — fixture 영향 평가.

- [ ] **Step 3.2: `get_feedback_evaluator` 를 request-scoped 로**

`alphapulse/webapp/api/feedback.py` 의 `get_feedback_evaluator` 를 다음과 같이 변경:

```python
def get_feedback_evaluator(request: Request) -> FeedbackEvaluator:
    """매 요청마다 새 FeedbackEvaluator 인스턴스를 생성한다.

    Evaluator 내부 record 캐시가 인스턴스 생명주기 = 요청 생명주기 내에서만
    유효하도록 request-scoped 로 구성. 싱글턴 대신 store 만 공유.
    """
    store = request.app.state.feedback_store
    return FeedbackEvaluator(store=store)
```

- [ ] **Step 3.3: main.py 에서 app.state.feedback_evaluator 제거**

`alphapulse/webapp/main.py` 에서:
1. `feedback_evaluator = FeedbackEvaluator(store=feedback_store)` 라인 삭제
2. `app.state.feedback_evaluator = feedback_evaluator` 라인 삭제
3. Import 도 더 이상 필요 없으면 제거 (다른 곳 사용 여부 grep)

Run 후 확인:
```bash
grep -n "FeedbackEvaluator\|feedback_evaluator" alphapulse/webapp/main.py
```
Expected: 관련 코드 없음 (또는 다른 이유로 남은 import 만).

- [ ] **Step 3.4: test fixture 영향 확인**

Run:
```bash
grep -rn "app.state.feedback_evaluator" tests/
```

매치가 있으면 해당 테스트에서 fixture 를 수정: `app.state.feedback_store` 를 설정하도록 확인 (이미 있을 가능성 높음). fixture 가 MagicMock 으로 `feedback_evaluator` 를 덮어쓰던 경우, 해당 테스트는 깨질 수 있으므로 `get_feedback_evaluator` 를 monkeypatch 하는 방식으로 전환하거나 fixture 를 수정.

**가장 안전한 조정 패턴** (tests/webapp/api/test_feedback.py 에 영향 있을 경우):

```python
# 기존
app.state.feedback_evaluator = some_mock

# 변경
from alphapulse.webapp.api import feedback as feedback_api
app.dependency_overrides[feedback_api.get_feedback_evaluator] = lambda: some_mock
```

- [ ] **Step 3.5: 전체 테스트**

Run: `pytest tests/webapp/ -q --tb=short`
Expected: 모두 통과.

- [ ] **Step 3.6: 린트 + 커밋**

```bash
ruff check alphapulse/webapp/
git add alphapulse/webapp/main.py alphapulse/webapp/api/feedback.py tests/
git commit -m "refactor(feedback): Evaluator request-scoped 전환 — 싱글턴 제거"
```

---

## Task 4: FeedbackEvaluator record-level 캐싱

**Files:**
- Modify: `alphapulse/feedback/evaluator.py`
- Modify: `tests/feedback/test_evaluator.py`

- [ ] **Step 4.1: 캐시 동작 테스트 추가**

`tests/feedback/test_evaluator.py` 끝에 추가:

```python
def test_evaluator_caches_store_get_recent(evaluator, store, monkeypatch):
    """동일 limit 으로 get_* 메서드 여러 번 호출 시 store.get_recent 은 1회만 호출."""
    _seed_data(store, 5)
    call_count = {"n": 0}
    original = store.get_recent

    def counting(*args, **kwargs):
        call_count["n"] += 1
        return original(*args, **kwargs)

    monkeypatch.setattr(store, "get_recent", counting)

    evaluator.get_hit_rates(days=30)
    evaluator.get_hit_rates(days=30)   # cache hit
    evaluator.get_correlation(days=30)  # cache hit (same limit)
    evaluator.get_hit_rate_trend(days=30)  # cache hit

    assert call_count["n"] == 1


def test_evaluator_cache_separate_per_limit(evaluator, store, monkeypatch):
    """다른 limit 은 별도 캐시 키로 동작한다."""
    _seed_data(store, 30)
    call_count = {"n": 0}
    original = store.get_recent

    def counting(*args, **kwargs):
        call_count["n"] += 1
        return original(*args, **kwargs)

    monkeypatch.setattr(store, "get_recent", counting)

    evaluator.get_hit_rates(days=7)
    evaluator.get_hit_rates(days=30)
    evaluator.get_hit_rates(days=7)   # cache hit

    assert call_count["n"] == 2  # 7일과 30일 각 1회
```

- [ ] **Step 4.2: Red**

Run: `pytest tests/feedback/test_evaluator.py -k "caches\|cache_separate" -v`
Expected: FAIL — 현재는 매번 store.get_recent 호출.

- [ ] **Step 4.3: 구현**

`alphapulse/feedback/evaluator.py` 의 `FeedbackEvaluator.__init__` 를 다음과 같이 수정:

```python
    def __init__(self, store: FeedbackStore | None = None, db_path=None):
        if store:
            self.store = store
        else:
            from alphapulse.core.config import Config
            cfg = Config()
            self.store = FeedbackStore(db_path or (cfg.DATA_DIR / "feedback.db"))
        # Request-scoped cache: (limit,) → records. 인스턴스 생명주기 = request.
        self._records_cache: dict[int, list[dict]] = {}
```

동일 파일에 private 헬퍼 추가 (어디든 `class FeedbackEvaluator` 내부, 기존 메서드 앞):

```python
    def _get_cached_records(self, limit: int) -> list[dict]:
        """store.get_recent(limit=limit) 결과를 인스턴스 캐시로 재사용."""
        if limit not in self._records_cache:
            self._records_cache[limit] = self.store.get_recent(limit=limit)
        return self._records_cache[limit]
```

기존 메서드 4개의 `self.store.get_recent(limit=...)` 호출을 `self._get_cached_records(...)` 로 교체:
- `get_hit_rates(self, days=30)`: `records = self._get_cached_records(days)`
- `get_indicator_accuracy(self, days=30, threshold=50.0)`: `records = self._get_cached_records(days)`
- `get_correlation(self, days=30)`: `records = self._get_cached_records(days)`
- `get_hit_rate_trend(self, days=30, window=7)`: `records = self._get_cached_records(days)`
- `get_score_return_points(self, days=30)`: `records = self._get_cached_records(days)`
- `get_indicator_heatmap(self, days=30)`: `records = self._get_cached_records(days)`
- `get_signal_breakdown(self, days=30)`: `records = self._get_cached_records(days)`

(즉, `self.store.get_recent(limit=...)` 직접 호출을 모두 내부 헬퍼로 통일)

- [ ] **Step 4.4: Green**

Run: `pytest tests/feedback/test_evaluator.py -v`
Expected: 전체 통과 (신규 2 + 기존 19).

- [ ] **Step 4.5: 린트 + 커밋**

```bash
ruff check alphapulse/feedback/evaluator.py tests/feedback/test_evaluator.py
git add alphapulse/feedback/evaluator.py tests/feedback/test_evaluator.py
git commit -m "perf(feedback): Evaluator record-level cache (_get_cached_records)"
```

---

## Task 5: `get_all_analytics` one-pass 메서드

**Files:**
- Modify: `alphapulse/feedback/evaluator.py`
- Modify: `tests/feedback/test_evaluator.py`

- [ ] **Step 5.1: 골든 테스트 추가**

`tests/feedback/test_evaluator.py` 끝에 추가:

```python
def test_get_all_analytics_returns_four_keys(evaluator, store):
    _seed_data(store, 10)
    result = evaluator.get_all_analytics(days=30)
    assert set(result.keys()) == {
        "hit_rate_trend",
        "score_return_points",
        "indicator_heatmap",
        "signal_breakdown",
    }


def test_get_all_analytics_matches_individual_methods(evaluator, store):
    """기존 4 메서드와 동등 결과 — 골든 테스트."""
    _seed_data(store, 10)
    bundled = evaluator.get_all_analytics(days=30)
    individual = {
        "hit_rate_trend": evaluator.get_hit_rate_trend(days=30),
        "score_return_points": evaluator.get_score_return_points(days=30),
        "indicator_heatmap": evaluator.get_indicator_heatmap(days=30),
        "signal_breakdown": evaluator.get_signal_breakdown(days=30),
    }
    assert bundled == individual


def test_get_all_analytics_single_store_call(evaluator, store, monkeypatch):
    """get_all_analytics 는 store.get_recent 을 1회만 호출한다."""
    _seed_data(store, 10)
    call_count = {"n": 0}
    original = store.get_recent

    def counting(*args, **kwargs):
        call_count["n"] += 1
        return original(*args, **kwargs)

    monkeypatch.setattr(store, "get_recent", counting)
    evaluator.get_all_analytics(days=30)
    assert call_count["n"] == 1
```

- [ ] **Step 5.2: Red**

Run: `pytest tests/feedback/test_evaluator.py -k "get_all_analytics" -v`
Expected: FAIL — 메서드 없음.

- [ ] **Step 5.3: 구현**

`alphapulse/feedback/evaluator.py` 의 `FeedbackEvaluator` 클래스 끝에 추가:

```python
    def get_all_analytics(self, days: int = 30, *, window: int = 7) -> dict:
        """4개 분석 데이터셋을 단일 get_recent 호출로 생성한다.

        /api/v1/feedback/analytics 전용 엔드포인트용. 기존 4 메서드와
        동등한 결과를 반환 (골든 테스트로 검증).
        """
        # 단일 get_recent 호출 — _get_cached_records 가 캐시에 저장
        records = self._get_cached_records(days)
        return {
            "hit_rate_trend": self._compute_hit_rate_trend(records, window),
            "score_return_points": self._compute_score_return_points(records),
            "indicator_heatmap": self._compute_indicator_heatmap(records),
            "signal_breakdown": self._compute_signal_breakdown(records),
        }

    @staticmethod
    def _compute_hit_rate_trend(records: list[dict], window: int) -> list[dict]:
        records_asc = list(reversed(records))
        result: list[dict] = []
        for i in range(len(records_asc)):
            win = records_asc[max(0, i - window + 1): i + 1]
            evaluated = [r["hit_1d"] for r in win if r["hit_1d"] is not None]
            avg = round(sum(evaluated) / len(evaluated), 4) if evaluated else None
            result.append({
                "date": records_asc[i]["date"],
                "rolling_hit_rate_1d": avg,
            })
        return result

    @staticmethod
    def _compute_score_return_points(records: list[dict]) -> list[dict]:
        return [
            {
                "date": r["date"],
                "score": float(r["score"]),
                "return_1d": float(r["return_1d"]),
                "signal": r["signal"],
            }
            for r in records
            if r["return_1d"] is not None
        ]

    @staticmethod
    def _compute_indicator_heatmap(records: list[dict]) -> list[dict]:
        cells: list[dict] = []
        for record in records:
            raw = record["indicator_scores"]
            try:
                scores = json.loads(raw) if isinstance(raw, str) else raw
            except (json.JSONDecodeError, TypeError):
                logger.warning(
                    "indicator_heatmap: skip %s — JSON parse fail", record["date"]
                )
                continue
            if not isinstance(scores, dict):
                continue
            for key, value in scores.items():
                if value is None:
                    continue
                try:
                    cells.append({
                        "date": record["date"],
                        "indicator": key,
                        "score": float(value),
                    })
                except (TypeError, ValueError):
                    continue
        return cells

    @staticmethod
    def _compute_signal_breakdown(records: list[dict]) -> list[dict]:
        from collections import defaultdict
        groups: dict[str, list[dict]] = defaultdict(list)
        for r in records:
            groups[r["signal"]].append(r)

        def _rate(group: list[dict], key: str) -> float | None:
            vals = [r[key] for r in group if r[key] is not None]
            return round(sum(vals) / len(vals), 4) if vals else None

        return [
            {
                "signal": signal,
                "count": len(group),
                "hit_rate_1d": _rate(group, "hit_1d"),
                "hit_rate_3d": _rate(group, "hit_3d"),
                "hit_rate_5d": _rate(group, "hit_5d"),
            }
            for signal, group in groups.items()
        ]
```

기존 `get_hit_rate_trend`, `get_score_return_points`, `get_indicator_heatmap`, `get_signal_breakdown` 의 본문을 위 static helper 호출로 교체한다 (중복 로직 제거, DRY):

```python
    def get_hit_rate_trend(self, days: int = 30, window: int = 7) -> list[dict]:
        records = self._get_cached_records(days)
        return self._compute_hit_rate_trend(records, window)

    def get_score_return_points(self, days: int = 30) -> list[dict]:
        records = self._get_cached_records(days)
        return self._compute_score_return_points(records)

    def get_indicator_heatmap(self, days: int = 30) -> list[dict]:
        records = self._get_cached_records(days)
        return self._compute_indicator_heatmap(records)

    def get_signal_breakdown(self, days: int = 30) -> list[dict]:
        records = self._get_cached_records(days)
        return self._compute_signal_breakdown(records)
```

- [ ] **Step 5.4: Green**

Run: `pytest tests/feedback/test_evaluator.py -v`
Expected: 전체 통과 (신규 3 + 기존).

- [ ] **Step 5.5: 린트 + 커밋**

```bash
ruff check alphapulse/feedback/evaluator.py tests/feedback/test_evaluator.py
git add alphapulse/feedback/evaluator.py tests/feedback/test_evaluator.py
git commit -m "perf(feedback): get_all_analytics one-pass + _compute_* 헬퍼 분리"
```

---

## Task 6: `/analytics` 엔드포인트 get_all_analytics 사용

**Files:**
- Modify: `alphapulse/webapp/api/feedback.py`
- Modify: `tests/webapp/api/test_feedback.py`

- [ ] **Step 6.1: 기존 테스트 회귀 확인 준비**

기존 `/analytics` 테스트가 3건 있음 (test_analytics_returns_all_four_fields 등). 응답 shape 변경 없이 내부 호출만 교체 — 기존 테스트가 여전히 통과해야 한다.

- [ ] **Step 6.2: 구현**

`alphapulse/webapp/api/feedback.py` 의 `get_analytics` 엔드포인트를 다음과 같이 수정:

```python
@router.get("/analytics", response_model=AnalyticsResponse)
async def get_analytics(
    days: int = Query(30, ge=1, le=365),
    user: User = Depends(get_current_user),
    evaluator: FeedbackEvaluator = Depends(get_feedback_evaluator),
):
    """4개 시각화 데이터셋 번들 (one-pass)."""
    bundle = evaluator.get_all_analytics(days=days)
    return AnalyticsResponse(
        days=days,
        hit_rate_trend=[HitRateTrendPoint(**p) for p in bundle["hit_rate_trend"]],
        score_return_points=[ScoreReturnPoint(**p) for p in bundle["score_return_points"]],
        indicator_heatmap=[IndicatorHeatmapCell(**c) for c in bundle["indicator_heatmap"]],
        signal_breakdown=[SignalBreakdownRow(**r) for r in bundle["signal_breakdown"]],
    )
```

기존 4 메서드 개별 호출 부분이 `get_all_analytics` 1회 호출 + bundle unpacking 으로 교체된다.

- [ ] **Step 6.3: 회귀 + 성능 확인**

Run: `pytest tests/webapp/api/test_feedback.py -v`
Expected: 모두 통과.

선택: evaluator 의 `store.get_recent` 가 몇 번 호출되는지 확인하는 테스트 추가 (선택):

```python
def test_analytics_endpoint_single_store_call(client, feedback_store, monkeypatch):
    feedback_store.save_signal("20260401", 40.0, "매수 우위", {"investor_flow": 80})
    feedback_store.update_result("20260401", 2650, 1.0, 870, 0.5, 1.5, 1)

    call_count = {"n": 0}
    original = feedback_store.get_recent

    def counting(*args, **kwargs):
        call_count["n"] += 1
        return original(*args, **kwargs)

    monkeypatch.setattr(feedback_store, "get_recent", counting)
    r = client.get("/api/v1/feedback/analytics?days=30")
    assert r.status_code == 200
    assert call_count["n"] == 1
```

Run: `pytest tests/webapp/api/test_feedback.py::test_analytics_endpoint_single_store_call -v`
Expected: PASS — 1회만 호출 확인.

- [ ] **Step 6.4: 린트 + 커밋**

```bash
ruff check alphapulse/webapp/api/feedback.py tests/webapp/api/test_feedback.py
git add alphapulse/webapp/api/feedback.py tests/webapp/api/test_feedback.py
git commit -m "perf(webapp): /analytics get_all_analytics 단일 호출로 전환"
```

---

## Task 7: BriefingStore json_extract 경량 목록 + offset

**Files:**
- Modify: `alphapulse/core/storage/briefings.py`
- Modify: `tests/core/storage/test_briefings.py`

- [ ] **Step 7.1: 테스트 추가**

`tests/core/storage/test_briefings.py` 에 추가:

```python
def test_list_summaries_returns_only_summary_fields(store):
    payload = {
        "pulse_result": {"score": 62.5, "signal": "매수 우위"},
        "commentary": "x",
        "synthesis": "y",
        "daily_result_msg": "m",
        "news": {"articles": []},
    }
    store.save(date="20260420", payload=payload)
    result = store.list_summaries(limit=10)
    assert len(result) == 1
    row = result[0]
    assert row["date"] == "20260420"
    assert row["score"] == 62.5
    assert row["signal"] == "매수 우위"
    assert row["has_synthesis"] is True
    assert row["has_commentary"] is True
    assert "created_at" in row
    # payload 전체가 포함되지 않아야 한다
    assert "payload" not in row
    assert "news" not in row


def test_list_summaries_paginates_with_offset(store):
    for i in range(5):
        store.save(date=f"2026041{i}", payload={"pulse_result": {"score": float(i), "signal": "중립"}})
    page1 = store.list_summaries(limit=2, offset=0)
    page2 = store.list_summaries(limit=2, offset=2)
    page3 = store.list_summaries(limit=2, offset=4)
    assert [r["date"] for r in page1] == ["20260414", "20260413"]  # DESC
    assert [r["date"] for r in page2] == ["20260412", "20260411"]
    assert [r["date"] for r in page3] == ["20260410"]


def test_list_summaries_has_synthesis_false_when_missing(store):
    store.save(date="20260420", payload={"pulse_result": {"score": 0.0, "signal": "중립"}})
    result = store.list_summaries(limit=10)
    assert result[0]["has_synthesis"] is False
    assert result[0]["has_commentary"] is False


def test_get_recent_offset(store):
    for i in range(5):
        store.save(date=f"2026041{i}", payload={"test": i})
    first = store.get_recent(days=3, offset=0)
    second = store.get_recent(days=3, offset=3)
    assert [r["date"] for r in first] == ["20260414", "20260413", "20260412"]
    assert [r["date"] for r in second] == ["20260411", "20260410"]
```

- [ ] **Step 7.2: Red**

Run: `pytest tests/core/storage/test_briefings.py -k "list_summaries\|get_recent_offset" -v`
Expected: FAIL — 메서드/파라미터 없음.

- [ ] **Step 7.3: 구현**

`alphapulse/core/storage/briefings.py` 의 `BriefingStore.get_recent` 를 확장하고 `list_summaries` 를 추가.

기존 `get_recent(days)` 시그니처에 `offset: int = 0` 추가:

```python
    def get_recent(self, days: int = 30, offset: int = 0) -> list[dict]:
        """날짜 DESC 정렬, 최대 days 건. offset 으로 페이지 네이션."""
        with sqlite3.connect(self.db_path) as conn:
            conn.row_factory = sqlite3.Row
            rows = conn.execute(
                "SELECT date, payload, created_at FROM briefings "
                "ORDER BY date DESC LIMIT ? OFFSET ?",
                (days, offset),
            ).fetchall()
        return [
            {
                "date": r["date"],
                "payload": json.loads(r["payload"]),
                "created_at": r["created_at"],
            }
            for r in rows
        ]
```

`list_summaries` 신규 메서드 추가 (get_recent 아래):

```python
    def list_summaries(self, limit: int, offset: int = 0) -> list[dict]:
        """payload 의 요약 필드만 json_extract 로 추출 (경량 목록용).

        Returns:
            [{date, created_at, score, signal, has_synthesis, has_commentary}]
        """
        with sqlite3.connect(self.db_path) as conn:
            conn.row_factory = sqlite3.Row
            rows = conn.execute(
                """
                SELECT
                    date,
                    created_at,
                    json_extract(payload, '$.pulse_result.score') AS score,
                    json_extract(payload, '$.pulse_result.signal') AS signal,
                    (json_extract(payload, '$.synthesis') IS NOT NULL) AS has_synthesis,
                    (json_extract(payload, '$.commentary') IS NOT NULL) AS has_commentary
                FROM briefings
                ORDER BY date DESC
                LIMIT ? OFFSET ?
                """,
                (limit, offset),
            ).fetchall()
        return [
            {
                "date": r["date"],
                "created_at": r["created_at"],
                "score": float(r["score"]) if r["score"] is not None else 0.0,
                "signal": str(r["signal"]) if r["signal"] is not None else "",
                "has_synthesis": bool(r["has_synthesis"]),
                "has_commentary": bool(r["has_commentary"]),
            }
            for r in rows
        ]
```

- [ ] **Step 7.4: Green**

Run: `pytest tests/core/storage/test_briefings.py -v`
Expected: 전체 통과.

- [ ] **Step 7.5: 린트 + 커밋**

```bash
ruff check alphapulse/core/storage/briefings.py tests/core/storage/test_briefings.py
git add alphapulse/core/storage/briefings.py tests/core/storage/test_briefings.py
git commit -m "perf(briefings): BriefingStore.list_summaries (json_extract) + offset"
```

---

## Task 8: `/api/v1/briefings` 목록 엔드포인트 list_summaries 사용

**Files:**
- Modify: `alphapulse/webapp/api/briefing.py`
- Modify: `tests/webapp/api/test_briefing.py` (회귀)

- [ ] **Step 8.1: 엔드포인트 현행 확인**

Run: `grep -n "@router.get(\"\"\|@router.get(\"/?\"\|list_briefings\|get_recent" alphapulse/webapp/api/briefing.py | head -10`

목록 엔드포인트는 `@router.get("", response_model=BriefingListResponse)`. 내부에서 `store.get_recent(days=999)` 로 전체 로드 후 페이지네이션하는지 확인:

```bash
sed -n '125,145p' alphapulse/webapp/api/briefing.py
```

- [ ] **Step 8.2: 엔드포인트 수정**

`alphapulse/webapp/api/briefing.py` 의 목록 엔드포인트 (`@router.get("", response_model=BriefingListResponse)`) 를 다음과 같이 변경:

```python
@router.get("", response_model=BriefingListResponse)
async def list_briefings(
    page: int = Query(1, ge=1),
    size: int = Query(20, ge=1, le=100),
    _: User = Depends(get_current_user),
    store: BriefingStore = Depends(get_briefing_store),
):
    """브리핑 요약 목록 (json_extract 기반 경량 payload)."""
    offset = (page - 1) * size
    summaries = store.list_summaries(limit=size, offset=offset)
    # 총 건수 — json_extract 없는 단순 카운트
    import sqlite3
    with sqlite3.connect(store.db_path) as conn:
        total = conn.execute("SELECT COUNT(*) FROM briefings").fetchone()[0]
    return BriefingListResponse(
        items=[
            BriefingSummary(
                date=s["date"],
                score=s["score"],
                signal=s["signal"],
                has_synthesis=s["has_synthesis"],
                has_commentary=s["has_commentary"],
                created_at=s["created_at"],
            )
            for s in summaries
        ],
        page=page,
        size=size,
        total=total,
    )
```

기존에 `_row_to_summary(row)` 를 헬퍼로 쓰던 부분이 있으면 그대로 남겨두어도 되지만, list_summaries 로부터 바로 BriefingSummary 매핑하므로 대체 가능. 기존 헬퍼는 다른 곳에서 사용 여부 grep 후 남길지 결정:

```bash
grep -n "_row_to_summary" alphapulse/webapp/api/briefing.py
```

- [ ] **Step 8.3: 회귀**

Run: `pytest tests/webapp/api/test_briefing.py -v`
Expected: 모두 통과 (응답 shape 불변).

- [ ] **Step 8.4: 린트 + 커밋**

```bash
ruff check alphapulse/webapp/api/briefing.py
git add alphapulse/webapp/api/briefing.py tests/webapp/api/test_briefing.py
git commit -m "perf(briefings): /api/v1/briefings 목록 list_summaries(offset) 사용"
```

---

## Task 9: FeedbackStore offset + /history endpoint 적용

**Files:**
- Modify: `alphapulse/core/storage/feedback.py`
- Modify: `alphapulse/webapp/api/feedback.py`
- Modify: `tests/feedback/test_store.py` 또는 `tests/webapp/api/test_feedback.py`

- [ ] **Step 9.1: store offset 테스트 추가**

`tests/feedback/test_store.py` 끝에:

```python
def test_get_recent_offset(store):
    for i in range(5):
        date = f"2026040{i+1}" if i < 9 else f"202604{i+1}"
        store.save_signal(date, float(i * 10), "매수 우위", {})
    first = store.get_recent(limit=2, offset=0)
    second = store.get_recent(limit=2, offset=2)
    third = store.get_recent(limit=2, offset=4)
    assert [r["date"] for r in first] == ["20260405", "20260404"]  # DESC
    assert [r["date"] for r in second] == ["20260403", "20260402"]
    assert [r["date"] for r in third] == ["20260401"]
```

- [ ] **Step 9.2: Red**

Run: `pytest tests/feedback/test_store.py::test_get_recent_offset -v`
Expected: FAIL — offset 없음.

- [ ] **Step 9.3: FeedbackStore 구현**

`alphapulse/core/storage/feedback.py` 의 `get_recent` 시그니처를 다음과 같이 변경:

```python
    def get_recent(self, limit: int = 30, offset: int = 0) -> list[dict]:
        """최근 N건의 피드백 레코드를 조회한다.

        signal_feedback.date 가 PRIMARY KEY 이므로 날짜당 1행. 거래일(신호가
        없는 주말/공휴일 제외)의 최근 `limit` 건을 반환. offset 으로 페이지네이션.
        """
        with sqlite3.connect(self.db_path) as conn:
            conn.row_factory = sqlite3.Row
            cursor = conn.execute(
                "SELECT * FROM signal_feedback "
                "ORDER BY date DESC "
                "LIMIT ? OFFSET ?",
                (limit, offset),
            )
            rows = cursor.fetchall()

        return [dict(row) for row in rows]
```

- [ ] **Step 9.4: Green on store**

Run: `pytest tests/feedback/test_store.py -v`
Expected: 전체 통과.

- [ ] **Step 9.5: /history 엔드포인트 offset 적용**

`alphapulse/webapp/api/feedback.py` 의 `get_history` 현 구현은 `rows = store.get_recent(limit=days)` → `rows[start:start+size]` in-memory slice. 이걸 DB offset 으로 변경:

```python
@router.get("/history", response_model=FeedbackHistoryResponse)
async def get_history(
    days: int = Query(30, ge=1, le=365),
    page: int = Query(1, ge=1),
    size: int = Query(20, ge=1, le=100),
    user: User = Depends(get_current_user),
    store: FeedbackStore = Depends(get_feedback_store),
):
    # 총 건수는 기간 내 레코드 기준 (days 는 기간 필터가 아니지만 관행 유지)
    # 성능상 total 은 limit=days 전체 조회 결과 length 로 근사
    total_rows = store.get_recent(limit=days)
    total = len(total_rows)
    offset = (page - 1) * size
    # limit 이 size 이지만, 페이지가 total 초과하지 않도록 store 에서 size 만큼만 조회
    page_rows = store.get_recent(limit=size, offset=offset)
    # days 외곽 필터링: total_rows 의 마지막 "days" 만큼만 유효하므로 offset >= days 이면 빈 페이지
    if offset >= days:
        page_rows = []

    return FeedbackHistoryResponse(
        items=[
            SignalHistoryItem(
                date=r["date"],
                score=float(r["score"]),
                signal=r["signal"],
                kospi_change_pct=r["kospi_change_pct"],
                return_1d=r["return_1d"],
                return_3d=r["return_3d"],
                return_5d=r["return_5d"],
                hit_1d=_int_to_bool(r["hit_1d"]),
                hit_3d=_int_to_bool(r["hit_3d"]),
                hit_5d=_int_to_bool(r["hit_5d"]),
            )
            for r in page_rows
        ],
        page=page,
        size=size,
        total=min(total, days),
    )
```

**주의**: 기존 동작 유지를 위해 `total` 계산에 한 번 `get_recent(limit=days)` 호출이 남아있다. 이는 evaluator 캐시와는 별개 (엔드포인트가 별개 요청, evaluator 가 여기서 안 씀). 추후 개선 여지 있지만 이 PR 에선 동작 호환이 우선.

- [ ] **Step 9.6: 회귀**

Run: `pytest tests/webapp/api/test_feedback.py -k "history" -v`
Expected: 기존 history 테스트 통과 유지.

- [ ] **Step 9.7: 린트 + 커밋**

```bash
ruff check alphapulse/core/storage/feedback.py alphapulse/webapp/api/feedback.py
git add alphapulse/core/storage/feedback.py alphapulse/webapp/api/feedback.py tests/feedback/test_store.py
git commit -m "perf(feedback): FeedbackStore.get_recent offset + /history DB 페이지네이션"
```

---

## Task 10: 전체 CI Gate 검증

**목적**: 모든 변경 종합 확인 + 회귀 없음.

- [ ] **Step 10.1: pytest 전체**

```bash
cd /Users/gwangsoo/alpha-pulse
pytest tests/ -x -q --tb=short 2>&1 | tail -5
```
Expected: 1272+ passed (QW-2 스킵 반영).

- [ ] **Step 10.2: ruff**

```bash
ruff check alphapulse/
```
Expected: All checks passed!

- [ ] **Step 10.3: FE 빌드**

```bash
cd /Users/gwangsoo/alpha-pulse/webapp-ui
pnpm lint
pnpm build 2>&1 | tail -10
```
Expected: 빌드 성공 (이 PR 은 BE 변경 위주라 FE 는 영향 없음).

- [ ] **Step 10.4: 커밋 없음** — 검증 통과 시 병합 단계.

---

## Spec Coverage 체크

- [x] Spec §2.1 Item #1 (Evaluator N+1) → Task 5, 6
- [x] Spec §2.1 Item #2 (request-scoped + 메모이제이션) → Task 3, 4
- [x] Spec §2.1 Item #3 (DB 페이지네이션) → Task 7, 9
- [x] Spec §2.1 Item #4 (ContentReader 캐시) → Task 2
- [x] Spec §2.1 Item #5 (Briefing 경량 payload) → Task 7 (list_summaries)
- [x] Spec §2.2 QW-1 (인덱스) → Task 1
- [N/A] Spec §2.2 QW-2 (pulse history 서버 정렬) → 이미 구현됨 확인, 스킵
- [deferred] Spec §2.2 QW-3 (revalidate) → 별도 브랜치로 분리 (인증 쿠키 충돌 위험, 본 PR 범위 밖으로 이동)
- [x] Spec §6 테스트 → 각 Task 에 단위 + 회귀 포함

## Implementation Notes

1. **Task 순서 엄격 준수**: 3 (request-scoped) → 4 (캐시) → 5 (get_all_analytics) → 6 (endpoint 사용). Task 3 없이 Task 4 캐시를 싱글턴에 넣으면 cross-request stale 발생.
2. **Task 7** 이 Task 8 을 enable — list_summaries 먼저, API 적용은 다음.
3. **Task 9** 는 7/8 과 독립 — 병렬로 진행 가능 (실제로는 subagent 1개씩 순차).
4. **QW-3 revalidate 는 본 PR 범위 밖**: FE fetch 가 쿠키 헤더를 수동 설정하므로 Next.js 의 `fetch` 캐시와 호환 복잡. 별도 작업으로 분리. 본 PR 에서는 측정 가능한 백엔드 개선에 집중.
5. **측정**: CI Gate 이후 `/feedback?days=365`, `/briefings`, `/content` 3 개 페이지 TTFB 를 수동 측정 → PR 설명에 기록.
6. **호환성**: 모든 응답 shape 불변. 기존 FE 변경 없음.
